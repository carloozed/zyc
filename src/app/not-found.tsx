import NotFoundContent from '@/app/components/NotFound/NotFoundContent';

// Fallback for anything the [lang] segment doesn't catch.
export default function NotFound() {
  return <NotFoundContent />;
}
